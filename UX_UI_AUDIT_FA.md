# گزارش جامع ارزیابی UI/UX پلتفرم

تاریخ ارزیابی: ۲۴ اوت ۲۰۲۶  
دامنه: `platform-core`، `platform-theme` و `platform-example`  
نوع ارزیابی: بررسی محصول از نگاه کاربر نهایی، مدیر و ادمین؛ همراه با بررسی کد رابط، تست اجرایی و تست خودکار دسترس‌پذیری

## خلاصه مدیریتی

پروژه از نظر معماری فنی، تفکیک نقش‌ها و داشتن یک Design System اولیه، پایه قابل‌استفاده‌ای دارد؛ اما در وضعیت فعلی هنوز تجربه یک «محصول آماده» را منتقل نمی‌کند. ظاهر عمدتاً از کارت‌های سفید، حاشیه‌های خاکستری و متن تشکیل شده و هویت بصری، سلسله‌مراتب، عمق، تصویرسازی و نشانه‌های تعاملی کافی ندارد. از نظر تجربه کاربری نیز چند مسیر حیاتی یا مسدود است یا رفتار قابل‌اعتماد و شفافی ندارد.

مهم‌ترین نتیجه این ارزیابی این است که مشکل فقط زیبایی نیست. سه مانع بحرانی وجود دارد:

1. در نصب تازه، سطح دسترسی نقش‌های پیش‌فرض User و Manager به‌اشتباه با مقدار `0` ذخیره می‌شود؛ در نتیجه کاربر و مدیر پس از ورود به تمام مسیرهای اصلی با 403 روبه‌رو می‌شوند.
2. در موبایل، سایدبار مخفی می‌شود اما دکمه بازکردن منو نیز به‌دلیل ترتیب CSS مخفی باقی می‌ماند؛ بنابراین ناوبری پنل، تغییر پنل، زبان، پروفایل و خروج عملاً در دسترس نیست.
3. تغییر زبان به فارسی، فقط `lang="fa-IR"` را عوض می‌کند؛ رابط همچنان انگلیسی است و `dir="rtl"` نیز اعمال نمی‌شود. ادعای «پشتیبانی بومی RTL/LTR» در صفحه اصلی با رفتار واقعی محصول هم‌خوان نیست.

بنابراین بازطراحی ظاهری بدون رفع فاز صفر، فقط لایه‌ای زیبا روی تجربه‌ای مسدود و غیرقابل‌اعتماد خواهد بود.

## امتیاز وضعیت فعلی

| حوزه | امتیاز از ۱۰ | جمع‌بندی |
|---|---:|---|
| آمادگی عملکردی مسیرهای اصلی | ۳ | ورود کار می‌کند، اما دسترسی نقش‌های اصلی در نصب تازه شکسته است |
| معماری اطلاعات و ناوبری | ۴ | ساختار نقش‌محور مشخص است، ولی مسیرها تکراری و موبایل مسدود است |
| تجربه کاربری عمومی | ۳ | بازخورد، راهنمایی، حالات و پیوستگی جریان‌ها ناکافی است |
| تجربه پنل مدیریت | ۴ | قابلیت‌های اصلی وجود دارد، ولی متراکم، فنی و پرریسک است |
| طراحی بصری و هویت | ۴ | توکن و کامپوننت اولیه وجود دارد، ولی خروجی بی‌روح و یکنواخت است |
| تجربه موبایل | ۲ | محتوای اصلی واکنش‌گراست، اما ناوبری پنل در دسترس نیست |
| دسترس‌پذیری | ۶ | صفحات عمومی خوب‌اند؛ فرم‌ها، کنتراست و کنترل‌های پنل ایراد دارند |
| فارسی‌سازی و RTL | ۱ | سوییچر وجود دارد، ولی ترجمه و RTL در اجرای واقعی اعمال نمی‌شود |
| اعتماد و شفافیت سیستم | ۳ | چند تنظیم ذخیره می‌شود اما اثری در محصول ندارد؛ بازخورد عملیات ناقص است |
| امتیاز کلی | **۳٫۳** | مناسب برای Foundation فنی، نامناسب برای عرضه به کاربر نهایی |

## روش و محدوده ارزیابی

### محیط تست

- نصب تازه WordPress با فعال‌سازی `platform-core`، `platform-theme` و `platform-example`
- سه حساب واقعی با نقش‌های Platform User، Platform Manager و Platform Admin
- تست در عرض دسکتاپ `1440×900` و موبایل `390×844`
- بررسی مسیرهای عمومی، احراز هویت، پنل کاربر، پنل مدیر، پنل ادمین، پروفایل، تنظیمات، ACL، نقش‌ها، مجوزها، ماژول‌ها و صفحات خطا
- تست خودکار WCAG 2 A/AA با axe و بررسی Lighthouse روی صفحات عمومی
- بررسی PHP، قالب‌ها، CSS، JavaScript، مسیریابی، فرم‌ها و ACL

### نکته مهم درباره ادامه تست

در نصب تازه، هر دو نقش User و Manager در تمام مسیرهای مجاز خود 403 دریافت کردند. بررسی دیتابیس نشان داد `scope_type` مجوزهای این نقش‌ها به‌جای `all` با مقدار `0` ذخیره شده است. برای اینکه ارزیابی سایر صفحات ممکن شود، فقط در محیط تست موقت مقدارهای دیتابیس به `all` اصلاح شد. این تغییر بخشی از مخزن یا محصول نیست.

### محدودیت

ابزار مرجع Mobbin در این اجرا در دسترس نبود. مقایسه الگوها بر اساس قواعد شناخته‌شده داشبوردهای SaaS، WCAG 2.1 AA، الگوهای مدیریت دسترسی و شواهد خود محصول انجام شده است.

## نقشه محصول و سفرهای بررسی‌شده

### کاربر عمومی

`Home → Register/Login → App Launcher → User Panel → Profile/Settings → Logout`

### مدیر

`Login → App Launcher → Manager Panel → Team/Module Overview → Manager Profile/Settings`

### ادمین

`Login → Admin Dashboard → Users → User Detail/Role Assignment → Roles → Permissions/Scopes → Modules → ACL/Audit → Settings`

### نتیجه سفرها

| سفر | وضعیت | مسئله اصلی |
|---|---|---|
| ثبت‌نام و ورود | نیمه‌موفق | فرم‌ها کار می‌کنند، اما خطاها عمومی و بدون نگه‌داشت داده‌اند |
| ورود User به پنل | ناموفق در نصب تازه | خطای ACL و نمایش 403 |
| ورود Manager به پنل | ناموفق در نصب تازه | خطای ACL و نمایش 403 |
| ورود Admin | موفق | مدیر WordPress از مسیر bypass مجوزهای Core عبور می‌کند |
| ویرایش پروفایل | موفق ولی قابل‌بهبود | پیام نمایش داده می‌شود، اما URL پاک و Focus مدیریت نمی‌شود |
| تنظیم اعلان کاربر عادی | ناممکن | نقش پیش‌فرض User مجوز `core.settings.edit` ندارد |
| مدیریت نقش و مجوز | قابل‌استفاده ولی پرریسک | فرم‌های ریز، عملیات فوری، برچسب و کنتراست ناکافی |
| استفاده در موبایل | ناقص | منوی پنل و ابزارهای هدر قابل دسترسی نیستند |
| تغییر زبان به فارسی | ناموفق | متن انگلیسی باقی می‌ماند و RTL فعال نمی‌شود |

## نقاط قوت

- جداسازی روشن Core، Theme و Example Module
- تعریف Design Token برای رنگ، فاصله، شعاع، سایه و تایپوگرافی
- استفاده مناسب از `focus-visible` در دکمه‌ها و ورودی‌ها
- وجود حالت Empty State، Alert، Badge، Stat Card و Page Header
- تفکیک پنل User، Manager و Admin با ACL
- استفاده از Nonce و کنترل مجوز در عملیات حساس
- وجود Audit Log و توضیح Effective Access
- صفحات عمومی در تست Lighthouse امتیاز 100 دسترس‌پذیری گرفتند
- فرم‌های ورود و ثبت‌نام دارای autocomplete و دکمه نمایش رمز هستند
- طراحی بدون Layout Shift محسوس؛ مقدار CLS در تست صفحات عمومی صفر بود

این نقاط قوت نشان می‌دهد بازطراحی لازم نیست معماری را دور بریزد؛ باید لایه محصول، رفتار و Design System فعلی را تکمیل و یکپارچه کند.

## یافته‌های بحرانی — P0

### P0-01 — ذخیره تمام Permission Grantهای جدید خراب است

- **مشاهده واقعی:** پس از اختصاص صحیح `platform_user` و `platform_manager`، مسیرهای `/app`، `/app/user`، `/app/manager`، `/profile` و `/settings` همگی 403 برگرداندند.
- **علت:** ترتیب آرایه داده در `RoleManager::assign_permission()` با ترتیب formatهای `$wpdb->insert()` هم‌خوان نیست. `scope_type='all'` با `%d` ذخیره و به `0` تبدیل می‌شود.
- این نقص محدود به Seed نصب تازه نیست؛ هر Grant جدید از Admin UI یا REST API نیز از همین متد عبور می‌کند و `scope_type` و در صورت وجود `scope_value` را نادرست ذخیره می‌کند.
- **شاهد کد:** `platform-core/includes/ACL/RoleManager.php` خطوط 190 تا 212.
- **اثر کاربری:** کاربر با موفقیت ثبت‌نام یا وارد می‌شود، اما هیچ پنلی در دسترس ندارد.
- **اقدام لازم:** اصلاح ترتیب formatها، بازگردانی Default Grantهای شناخته‌شده به `all` و بازسازی سایر Grantهای خراب از Audit Log یا Revoke برای بازبینی ادمین. تبدیل کور همه مقدارهای `0` به `all` خطر افزایش ناخواسته دسترسی را دارد.
- **معیار پذیرش:** پس از نصب صفر تا صد، User و Manager بدون اصلاح دستی دیتابیس به تمام مسیرهای مجاز وارد شوند و مسیر غیرمجاز 403 صحیح بدهد.

### P0-02 — ناوبری پنل در موبایل غیرقابل‌دسترسی است

- در breakpoint زیر 900px، `.mpp-header__tools` و `.mpp-sidebar` مخفی می‌شوند.
- داخل Media Query، دکمه `.mpp-nav-toggle` نمایان می‌شود؛ اما تعریف عمومی `display:none` بعد از Media Query قرار گرفته و آن را دوباره مخفی می‌کند.
- تست محاسباتی در عرض 390px تأیید کرد دکمه دیده نمی‌شود، در حالی‌که سایدبار وجود دارد و مخفی است.
- حتی در صورت نمایش دکمه، `aria-controls="mpp-main-content"` است؛ در حالی‌که کنترل واقعی سایدبار است و سایدبار `id` ندارد.
- **شاهد کد:** `platform-theme/assets/css/panels.css` خطوط 422 تا 491 و `platform-theme/header.php` خطوط 59 تا 61.
- **اثر:** کاربر موبایل به Dashboardهای دیگر، Profile، Settings، Language و Logout دسترسی پایدار ندارد.
- **اقدام لازم:** پیاده‌سازی Drawer موبایل با focus trap، دکمه بستن، Escape، overlay، `aria-controls` صحیح و حفظ ابزارهای حساب.

### P0-03 — فارسی و RTL در محصول واقعی کار نمی‌کند

- **مشاهده واقعی:** پس از انتخاب فارسی، مقدار `lang` به `fa-IR` تغییر کرد، اما `dir` خالی ماند و عنوان، منوها و محتوای صفحه همچنان انگلیسی بود.
- فقط فایل‌های `.po` در مخزن وجود دارند و خروجی قابل بارگذاری `.mo` یا PHP translation catalog وجود ندارد.
- بخش بزرگی از `msgstr`های فایل‌های فارسی نیز همان متن انگلیسی است و هنوز ترجمه نشده است.
- کاربر خارج‌شده در Landing، Login و Register اصلاً به Language Switcher دسترسی ندارد.
- کلاس RTL روی Body نیز به `is_rtl()` وابسته است و در اجرای تست فعال نشد.
- **شاهد کد:** `platform-theme/inc/design-system/class-language-switcher.php` و پوشه‌های `languages/`.
- **اثر:** رابط ترکیبی و ادعای نادرست پشتیبانی دو‌زبانه؛ برای کاربر فارسی‌زبان محصول عملاً ترجمه نشده است.
- **اقدام لازم:** Pipeline ساخت ترجمه، بارگذاری catalog، تعیین direction بر اساس locale انتخابی، تست visual regression برای RTL و بازبینی کامل متن فارسی.

## یافته‌های با اولویت بالا — P1

### P1-01 — تنظیمات ادمین ظاهراً کار می‌کنند، اما بخش مهمی از آن‌ها مصرف نمی‌شود

موارد زیر ذخیره می‌شوند ولی در مسیر واقعی محصول استفاده مؤثر ندارند:

- `platform_name`: هدر همچنان `bloginfo('name')` را نمایش می‌دهد.
- `default_dashboard`: ورود و فرم Login همچنان به `/app` هدایت می‌شوند.
- `session_remember_days`: چرخه Cookie وردپرس با این مقدار تنظیم نمی‌شود.
- `date_format`: صفحات از `get_option('date_format')` وردپرس استفاده می‌کنند.

**شاهد:** `PlatformSettings.php`، `AuthIntegration.php`، `header.php`، `page-profile.php` و `AdminRenderer.php`.

**اثر:** ادمین پیام «Settings saved» می‌بیند اما رفتار محصول تغییر نمی‌کند؛ این مسئله اعتماد را مستقیماً کاهش می‌دهد.

### P1-02 — بازخورد Profile و Settings ناقص است

Handler پس از ذخیره، `mpp_notice` و `mpp_message` را به URL اضافه و Account Layout پیام را با `role="alert"` رندر می‌کند؛ اما پارامترها در URL باقی می‌مانند، Focus به پیام منتقل نمی‌شود و Success نیز با semantics فوری Alert اعلام می‌شود.

**اثر:** بازخورد دیداری وجود دارد، ولی ممکن است کاربر Keyboard/Screen Reader آن را از دست بدهد یا با Refresh دوباره پیام قدیمی را ببیند.

**اقدام:** Success با `role="status"`، Error با `role="alert"`، Focus Management و پاک‌کردن Query String با History API یا Redirect دوم.

### P1-03 — کاربر عادی نمی‌تواند تنظیم اعلان خودش را تغییر دهد

Dashboard کاربر او را به Settings می‌فرستد و متن صفحه درباره مدیریت Preferences است؛ اما نقش پیش‌فرض User مجوز `core.settings.edit` ندارد. فرم برای User نمایش داده نمی‌شود و صفحه فقط View-only است.

**شاهد:** `Installer.php`، `panel-user.php` و `page-settings.php`.

حتی برای Manager که امکان ذخیره دارد، مقدار `mpp_notifications` فقط ذخیره و نمایش داده می‌شود و هیچ مصرف‌کننده اعلان/ایمیل در مخزن ندارد.

**پیشنهاد:** تفکیک «تنظیمات شخصی» از «تنظیمات سیستمی»، اعطای ویرایش preferences شخصی به همه کاربران معتبر و اتصال Preference اعلان به یک سرویس واقعی؛ در غیر این صورت کنترل فعلاً حذف شود.

### P1-04 — Card پایه padding ندارد و بخش بزرگی از رابط به حاشیه چسبیده است

`.mpp-card` فقط background، border، radius و shadow دارد. به‌جز Login و Placeholder، padding عمومی تعریف نشده است. تست computed style مقدار `0px` را در Profile، Manager Profile، Settings، Error Pages، Modules و Admin Settings تأیید کرد.

**اثر بصری:** فرم، تیتر و متن به لبه کارت می‌چسبد؛ ظاهر ناتمام، شلوغ و کم‌کیفیت می‌شود.

**اقدام:** ساخت Card API شامل Header/Body/Footer و variantهای compact/default/spacious.

### P1-05 — پنل Manager بیشتر Roadmap است تا ابزار قابل‌استفاده

Team Members و Pending Tasks غالباً `—` هستند؛ بخش Module Widgets خالی است و Team، Projects و Reports فقط پیام «در آینده» دارند.

**اثر:** Manager بعد از ورود ارزش عملی دریافت نمی‌کند و صفحه حس Demo/Prototype دارد.

**اقدام:** تا زمان وجود ماژول واقعی، بخش‌های آینده از Dashboard اصلی حذف یا به یک Empty State واحد با راهکار بعدی تبدیل شوند.

### P1-06 — نوار مدیریت WordPress در رابط محصول نمایش داده می‌شود

در تست حساب‌های واردشده، Admin Bar وردپرس بالای Shell محصول حاضر بود و متن‌هایی مثل About WordPress و Howdy را نشان داد.

**اثر:** هویت محصول دوپاره، فضای عمودی بیشتر، متن انگلیسی در حالت فارسی و دسترسی احتمالی کاربر به صفحات خارج از تجربه محصول.

**اقدام:** برای کاربران فرانت‌اند Platform، Admin Bar پنهان شود یا یک سیاست نقش‌محور صریح تعریف شود.

### P1-07 — هدر دسکتاپ متراکم و بدون اولویت روشن است

Panel Switcher، Language Select، Avatar، نام کاربر و Logout هم‌زمان در نوار 56px قرار گرفته‌اند. Panel Switcher حالت فعال قابل‌تشخیص ندارد و لینک‌ها در تست axe کنتراست ناکافی داشتند.

**پیشنهاد:** یک Workspace Switcher مشخص، یک منوی Account و یک کنترل Language؛ خروج از سطح اول هدر حذف و داخل Account Menu قرار گیرد.

### P1-08 — مدیریت Permission در موبایل overflow افقی دارد

در عرض 390px، صفحه `/app/admin/permissions` از viewport بزرگ‌تر شد. هر Permission شامل Badge، Select، Set Scope، Revoke/Grant و Details است و فرم‌های inline عرض زیادی می‌گیرند.

**اقدام:** الگوی Permission Drawer/Detail، اکشن منوی سه‌نقطه، نمایش Scope در خط مستقل و Toolbar چسبان.

### P1-09 — کنترل‌های فرم بدون نام دسترس‌پذیر هستند

axe موارد Critical زیر را ثبت کرد:

- Username غیرفعال در Profile و Manager Profile، label متصل ندارد.
- Select فیلتر Module در Permissions نام دسترس‌پذیر ندارد.
- Selectهای Scope در ردیف‌ها و کارت‌های Permission label ندارند.
- فیلدهای Create/Edit Role، Select مربوط به Add Role و Search کاربران نیز label متصل کافی ندارند.
- صفحه Skip Link برای رفتن مستقیم به محتوای اصلی ندارد.

**اقدام:** اتصال `for/id` یا `aria-label/aria-labelledby` معنادار، به‌خصوص با نام Permission مربوطه.

### P1-10 — کنتراست در ناوبری و اکشن‌های حساس کافی نیست

axe در دسکتاپ برای لینک‌های Panel Switcher، توضیح آیتم فعال سایدبار و برخی لینک‌ها خطای Serious ثبت کرد. در موبایل دکمه Revoke نیز کنتراست ناکافی داشت.

**ریشه طراحی:** استفاده مکرر از `--mpp-muted-foreground` روی `--mpp-muted` و danger کم‌رنگ.

**اقدام:** ساخت جدول contrast برای تمام Token/Stateها و الزام حداقل 4.5:1 برای متن عادی.

### P1-11 — عملیات مخرب در Admin محافظت UX کافی ندارد

- Remove Role و Revoke Permission بلافاصله Submit می‌شوند.
- فقط Delete Role از `window.confirm()` ساده استفاده می‌کند.
- پیام تأیید شامل نام Role/User/Permission و اثر تغییر نیست.
- Undo وجود ندارد.

**اقدام:** Confirm Dialog استاندارد با شرح اثر، نام شیء، سطح خطر و در عملیات پرتکرار امکان Undo.

### P1-12 — فرم‌های احراز هویت خطا را به‌صورت عمومی و دیرهنگام نشان می‌دهند

- روی فرم‌ها `novalidate` فعال است.
- خطا فقط بالای فرم نمایش داده می‌شود، نه کنار فیلد.
- Username/Email پس از خطا دوباره مقداردهی نمی‌شود.
- تطابق دو Password فقط پس از Submit سمت سرور بررسی می‌شود.
- معیار رمز قوی قبل از تایپ توضیح داده نشده است.

**اثر:** خطای بیشتر، ورود اطلاعات تکراری و تجربه ضعیف‌تر روی موبایل.

### P1-13 — بازیابی رمز کاربر را از Design System خارج می‌کند

لینک Forgot Password به جریان استاندارد `wp-login.php` می‌رود. ظاهر، Header، زبان و الگوی پیام آن با پلتفرم متفاوت است.

**اقدام:** صفحات Request/Check Email/Reset/Success داخل Theme و با همان Shell احراز هویت ساخته شوند.

### P1-14 — صفحات 403 و 404 مستقیم Status اشتباه دارند

درخواست مستقیم `/403` و `/404` هر دو HTTP 200 برگرداندند. فقط زمانی که Router به‌صورت داخلی `render_error()` را فراخوانی کند Status درست تنظیم می‌شود.

**اثر:** رفتار نادرست برای موتور جست‌وجو، مانیتورینگ، Cache و ابزارهای کمکی.

### P1-15 — ساختار بصری Landing Page طولانی، تکراری و بیش‌ازحد فنی است

صفحه اصلی ۹ بخش متوالی دارد که بیشتر آن‌ها با همان تیتر، متن و کارت خاکستری ساخته شده‌اند. عبارت‌هایی مانند ACL، Platform Core، Module Contract و Architecture برای کاربر نهایی ارزش مستقیم را توضیح نمی‌دهند.

**اثر:** صفحه از نظر ریتم یکنواخت و از نظر پیام نامتمرکز است؛ به‌جای معرفی یک محصول، مستند Foundation را نشان می‌دهد.

**پیشنهاد ساختار جدید:**

1. Hero با مسئله، ارزش و CTA اصلی
2. سه نتیجه قابل‌فهم برای کاربر/مدیر/سازمان
3. Preview واقعی پنل
4. اعتماد، امنیت و کنترل دسترسی با زبان ساده
5. ماژول‌ها/Use Caseها
6. CTA نهایی

## یافته‌های اولویت متوسط — P2

| کد | یافته | اثر | پیشنهاد |
|---|---|---|---|
| P2-01 | سایدبار فعال فقط class دارد و `aria-current="page"` ندارد | وضعیت فعلی برای Screen Reader روشن نیست | افزودن aria-current |
| P2-02 | Dashboard Launcher فقط نام پنل و فلش دارد | انتخاب بین پنل‌ها برای کاربر چندنقشی مبهم است | توضیح مسئولیت، آخرین فعالیت و مقصد |
| P2-03 | لینک‌های Header و Logo در موبایل Touch Target حداقلی ندارند | خطای لمس و استفاده سخت‌تر | حداقل 44×44px |
| P2-04 | 403/404 بدون padding، راهنمایی و مسیر جایگزین کافی‌اند | بن‌بست احساسی و عملی | تصویر/آیکن، توضیح علت و CTAهای زمینه‌ای |
| P2-05 | Breadcrumb فقط متن است | امکان بازگشت سلسله‌مراتبی ندارد | آیتم‌های قبلی لینک‌دار شوند |
| P2-06 | تاریخ‌ها خام و گاهی فنی‌اند | خوانایی پایین و ناسازگاری locale | Formatter مشترک و Relative Time |
| P2-07 | Activity Log کلیدهایی مثل `role.created` نشان می‌دهد | زبان داخلی سیستم به کاربر نشت می‌کند | Label انسانی + جزئیات قابل بازشدن |
| P2-08 | جدول‌ها Sort و Bulk Action ندارند | مدیریت تعداد زیاد کاربر/نقش کند می‌شود | sorting، selection و bulk workflow |
| P2-09 | Search/Filter با Submit کامل صفحه کار می‌کند | وقفه و از دست رفتن Context | حفظ فیلترها و progressive enhancement |
| P2-10 | فیلترهای Permissions و ACL متراکم‌اند | بار شناختی بالا | Filter Bar، chip فعال و Clear All |
| P2-11 | عملیات Permission پس از Redirect فقط role را حفظ می‌کند | Search و Module Filter از دست می‌رود | حفظ کامل state در redirect |
| P2-12 | Scopeهای Team/Department/Project/Organization به Context هدف نیاز دارند، اما Route Checkهای Core این Context را نمی‌دهند و Admin UI نیز Scope Value نمی‌گیرد | Scope انتخاب‌شده ممکن است همیشه Deny شود | تکمیل قرارداد Context/Value و غیرفعال‌سازی گزینه تا آن زمان |
| P2-13 | Empty Stateها عمدتاً فقط متن دارند | اقدام بعدی روشن نیست | CTA، توضیح علت و لینک راهنما |
| P2-14 | Loader، disabled-on-submit و جلوگیری از دوبار Submit وجود ندارد | احتمال عملیات تکراری | pending state و idempotency UX |
| P2-15 | Favicon و Meta Description وجود ندارد | هویت ضعیف در Tab/Search/Share | Brand assets و metadata |
| P2-16 | فونت از Google Fonts بارگیری می‌شود | وابستگی خارجی و تأخیر احتمالی | self-host و subset فارسی/لاتین |
| P2-17 | Admin Settings اصطلاحات فنی بدون Help Text کافی دارد | خطای پیکربندی | مثال، preview و توضیح اثر |
| P2-18 | متن Login و Register انگلیسی و عمومی است | حس محصول اختصاصی ایجاد نمی‌شود | microcopy برندمحور و contextual |
| P2-19 | Profile کاربر و Manager دو قالب تقریباً تکراری دارند | رفتار و نگه‌داری ناسازگار می‌شود | Account Experience مشترک |
| P2-20 | Manager و Admin نیز از Footer عمومی استفاده می‌کنند | فضای زائد در اپلیکیشن عملیاتی | Footer فشرده یا حذف در Shell داخلی |
| P2-21 | لینک Settings مدیر به `/settings` می‌رود و صفحه با Shell پنل User رندر می‌شود | Context مدیر ناگهان شکسته می‌شود | Route یا Shell زمینه‌محور برای تنظیمات مشترک |
| P2-22 | کلاس‌های استفاده‌شده‌ای مانند `.mpp-card--error`، `.mpp-alert--info`، `.mpp-checkbox`، `.mpp-field__hint` و `.mpp-table-wrap` Style متناظر ندارند | صفحات خطا، Hint، Checkbox و جدول‌ها ناتمام یا وابسته به Browser Default دیده می‌شوند | تکمیل Inventory کامپوننت‌ها و حذف کلاس‌های مرده |
| P2-23 | جدول‌های Admin با `text-align:left` و Back/Pagination با فلش‌های ثابت LTR ساخته شده‌اند | RTL در صفحات مدیریتی ناهماهنگ است | Logical Alignment و Iconهای direction-aware |
| P2-24 | Landing همیشه Register را نشان می‌دهد، حتی اگر ثبت‌نام عمومی غیرفعال باشد | CTA کاربر را به Redirect بی‌توضیح می‌فرستد | نمایش CTA بر اساس وضعیت واقعی Registration |

## تحلیل طراحی بصری

### چرا رابط «بی‌روح» دیده می‌شود؟

1. **رنگ تقریباً تک‌بعدی است:** سفید، خاکستری روشن و سرمه‌ای؛ رنگ accent یا هویت نقش‌ها وجود ندارد.
2. **همه‌چیز Card است:** Landing، Dashboard، Settings، Modules و Errorها زبان بصری مشابه دارند.
3. **سلسله‌مراتب ضعیف است:** تفاوت Heading، Section، KPI، Action و Help Text کم است.
4. **آیکن و تصویر وجود ندارد:** حتی ناوبری و KPIها فقط متن هستند.
5. **فاصله داخلی ناقص است:** Cardهای 0px padding ظاهر را ناتمام می‌کنند.
6. **Stateها ناقص‌اند:** Hover محدود است؛ Selected، Pressed، Loading، Skeleton و Toast سیستماتیک نیست.
7. **هویت برند حداقلی است:** Logo فقط حرف `P` در مربع تیره است.
8. **شخصیت پنل‌ها یکسان است:** User، Manager و Admin با وجود هدف متفاوت تقریباً یک Shell دارند.

### جهت پیشنهادی

- برند حرفه‌ای و آرام، نه تزئینی: یک Accent اصلی و رنگ‌های semantic کنترل‌شده
- Sidebar با آیکن، Active State واضح، بخش‌بندی کوتاه و قابلیت collapse در دسکتاپ
- KPI Card با آیکن، trend/context و action
- Surface hierarchy سه‌سطحی: Page، Section و Elevated/Overlay
- تایپوگرافی فارسی و لاتین با line-height و وزن‌های بهینه
- استفاده محدود از illustration در Landing، Empty State و Error
- Dark Mode فقط پس از تثبیت Tokenها؛ نه در فاز اول

## معماری اطلاعات پیشنهادی

### هدر

- Logo / نام محصول
- Workspace یا Panel Switcher واحد
- Search یا Command Palette در صورت وجود نیاز واقعی
- Language
- Account Menu: Profile، Settings، Help، Logout

### سایدبار User

- Overview
- Activity
- ماژول‌های نصب‌شده
- Profile
- Settings

### سایدبار Manager

- Overview
- Team
- Tasks
- Projects
- Reports
- ماژول‌ها

آیتمی که Backend ندارد نباید به‌عنوان مقصد مستقل نمایش داده شود.

### سایدبار Admin

- Overview
- Users
- Roles & Permissions
- Modules
- Audit Log
- Platform Settings

Roles و Permissions بهتر است یک حوزه واحد با Detail View و Tabهای داخلی باشند.

## پیشنهاد Design System

### Tokenها

- رنگ: Brand، Neutral، Success، Warning، Danger، Info با contrast تأییدشده
- فاصله: 4/8/12/16/24/32/48
- تایپوگرافی: Display، H1/H2/H3، Body، Label، Caption، Code
- Radius: کنترل‌ها، Cardها و Overlayها
- Elevation: Flat، Raised و Modal
- Motion: 120–200ms و پشتیبانی کامل `prefers-reduced-motion`

### کامپوننت‌های ضروری

- App Shell و Mobile Drawer
- Account Menu و Panel Switcher
- Button با loading
- Form Field با hint/error/success
- Toast/Flash Message
- Dialog و Confirm Dialog
- Table/Data Grid واکنش‌گرا
- Filter Bar و Active Filter Chips
- Card Header/Body/Footer
- Empty/Loading/Error State
- Pagination
- Tabs
- Badge و Status
- Permission Matrix/Permission Detail

### قواعد Content Design

- نام عملیات انسانی باشد، نه کلید داخلی.
- هر صفحه یک هدف اصلی و حداکثر یک Primary CTA داشته باشد.
- پیام خطا بگوید چه شد، چرا و قدم بعدی چیست.
- عملیات حساس قبل از اجرا اثر خود را توضیح دهد.
- Empty State به جای وعده آینده، راه بعدی ارائه دهد.

## فازبندی اصلاح

## فاز ۰ — بازکردن مسیرهای حیاتی و بازگرداندن اعتماد

**هدف:** محصول در نصب تازه و موبایل واقعاً قابل‌استفاده شود.

کارها:

1. اصلاح ذخیره `scope_type` و Migration داده‌های خراب
2. تست End-to-End نقش‌های User، Manager و Admin روی نصب تازه
3. اصلاح Mobile Drawer، دکمه منو و ARIA
4. تکمیل build/load ترجمه فارسی و RTL واقعی
5. اصلاح semantics، Focus و پاک‌سازی URL در Flash Messageهای Profile و Settings
6. اتصال تنظیمات ذخیره‌شده به رفتار واقعی یا حذف موقت آن‌ها
7. تصمیم روشن برای مجوز ویرایش تنظیمات شخصی User
8. اصلاح status صفحات 403/404
9. رفع labelهای Critical، کنتراست‌های Serious و افزودن Skip Link

**خروجی قابل پذیرش:**

- هر سه نقش، Journey اصلی خود را بدون بن‌بست طی کنند.
- ناوبری موبایل با لمس و Keyboard قابل استفاده باشد.
- رابط فارسی واقعاً ترجمه و RTL باشد.
- هیچ Setting نمایشی بدون اثر واقعی باقی نماند.

## فاز ۱ — بازطراحی معماری اطلاعات و Journeyها

**هدف:** کاهش سردرگمی و حذف مسیرهای نمایشی/تکراری.

کارها:

1. تعریف دقیق Persona و Job-to-be-done هر پنل
2. ساده‌سازی Header و تبدیل User Meta به Account Menu
3. ادغام منطقی Roles و Permissions
4. بازطراحی Dashboard هر نقش بر اساس کار واقعی
5. حذف Placeholderهای چندگانه Manager
6. ساخت جریان یکپارچه Forgot/Reset Password
7. حذف Admin Bar وردپرس از تجربه Platform
8. تعریف Navigation Map موبایل و دسکتاپ

**معیار پذیرش:**

- هر صفحه یک هدف اصلی مشخص داشته باشد.
- کاربر بدون حدس‌زدن بداند اکنون در کدام پنل و صفحه است.
- هیچ مقصدی صرفاً برای اعلام «در آینده» در منوی اصلی نباشد.

## فاز ۲ — بازطراحی بصری و تکمیل Design System

**هدف:** تبدیل Foundation خاکستری و بی‌روح به محصول منسجم و حرفه‌ای.

کارها:

1. تعریف هویت برند، Logo، Accent و سیستم Illustration
2. اصلاح Color/Type/Spacing Tokenها
3. ساخت Card استاندارد با padding و anatomy مشخص
4. آیکن‌گذاری ناوبری، KPI و Actionها
5. طراحی Stateهای Hover/Focus/Active/Disabled/Loading
6. بازطراحی Landing Page بر اساس ارزش کاربر
7. بازطراحی Auth، Error و Empty Stateها
8. self-host فونت و بهینه‌سازی فونت فارسی

**معیار پذیرش:**

- تمام صفحات فقط از کامپوننت‌های سیستم استفاده کنند.
- contrast تمام حالت‌ها WCAG AA باشد.
- صفحات در 390، 768، 1024 و 1440 بدون overflow ناخواسته باشند.

## فاز ۳ — بهبود تجربه Admin و عملیات پرریسک

**هدف:** مدیریت ACL برای ادمین سریع، قابل‌فهم و ایمن شود.

کارها:

1. Permission Detail و Role Detail یکپارچه
2. Search، Sort، Filter، Bulk Action و حفظ State
3. Confirm Dialog و Undo برای تغییرات حساس
4. نمایش اثر تغییر مجوز: چه کاربران و مسیرهایی متأثر می‌شوند
5. تکمیل Context/Value Scopeهای محدود و غیرفعال‌کردن آن‌ها تا زمان قابل‌استفاده‌شدن
6. تبدیل کلیدهای Audit به متن انسانی و Detail قابل‌بررسی
7. Responsive Data Grid یا Card View واقعی
8. Activity و Audit با timezone/locale صحیح

**معیار پذیرش:**

- ادمین بتواند قبل از Revoke اثر تغییر را ببیند.
- هیچ عملیات مخربی با یک کلیک ناخواسته نهایی نشود.
- Filter و Scroll Position پس از عملیات حفظ شوند.

## فاز ۴ — تثبیت کیفیت و سنجش مستمر

**هدف:** جلوگیری از بازگشت ایرادها.

کارها:

1. تست Playwright برای Journey هر نقش
2. axe در CI برای جلوگیری از بازگشت ایرادهای عمومی و authenticated
3. Visual Regression برای LTR/RTL و چهار breakpoint
4. تست نصب تازه و Migration دیتابیس
5. بودجه Performance و self-host assets
6. تست Keyboard و Screen Reader روی Auth، Navigation و Permission Matrix
7. تست Content و ترجمه توسط کاربر فارسی‌زبان
8. ثبت رویدادهای قیف Login/Register/Panel Switch/Save/Error

**شاخص‌های پیشنهادی:**

- نرخ موفقیت Taskهای اصلی بالاتر از 95٪
- صفر خطای axe با impact نوع Critical/Serious
- LCP صفحه عمومی زیر 2.5s در محیط Production
- خطای فرم ثبت‌نام و ورود کمتر از 10٪
- زمان اختصاص Role و Permission برای ادمین کمتر از مسیر فعلی و بدون خطای ناخواسته

## ترتیب پیشنهادی اجرا

1. P0-01، P0-02 و P0-03
2. دسترس‌پذیری Critical/Serious، بازخورد فرم‌ها و حذف تنظیمات نمایشی بدون اثر
3. محافظت عملیات مخرب ACL و Scopeهای غیرقابل‌استفاده
4. Information Architecture و Journeyها
5. Design System و Shell
6. Landing/Auth/User
7. Manager
8. Admin/ACL
9. RTL و Regression Gate

## مواردی که نباید قبل از فاز صفر انجام شوند

- Dark Mode
- انیمیشن‌های تزئینی
- افزودن Widgetهای بیشتر به Dashboard
- تنوع رنگ بدون تست contrast
- افزودن Scope یا Role جدید
- توسعه Landing طولانی‌تر

این موارد مشکل اصلی را حل نمی‌کنند و هزینه نگه‌داری را قبل از تثبیت تجربه پایه بالا می‌برند.

## نتیجه نهایی

پروژه یک Foundation فنی نسبتاً منظم دارد، اما هنوز یک محصول منسجم برای کاربر نیست. حس «بی‌روح و بی‌نظم» فقط از رنگ و ظاهر نمی‌آید؛ ریشه آن ترکیبی از Cardهای ناقص، محتوای فنی، Placeholderهای زیاد، ناوبری متراکم، بازخورد ضعیف و چند شکست عملکردی مهم است.

بهترین مسیر، بازطراحی یک‌باره همه صفحات نیست. ابتدا باید دسترسی، موبایل، فارسی و اعتماد عملیاتی در فاز صفر اصلاح شود؛ سپس Journey و معماری اطلاعات تثبیت شود و در ادامه Design System جدید روی آن قرار گیرد. با این ترتیب، تغییر ظاهری به بهبود واقعی تجربه تبدیل می‌شود و دوباره‌کاری کاهش پیدا می‌کند.
